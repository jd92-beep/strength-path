# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**Read `AGENTS.md` first — its guidance applies equally to Claude Code.** It is the single source of truth for this repo's commands, architecture, and constraints.

Workflow routing (which skill owns which phase, skill discovery, context boundaries) lives in the global handbook at `/Users/tommy/.claude/CLAUDE.md`. Route every task through it; do not open `using-superpowers` as a startup habit.

This file used to duplicate `AGENTS.md` verbatim, which meant the two drifted. Add repo guidance to `AGENTS.md` only.
